function dummy
    # Until I have a real linux-only fish function, this is a dummy function that will force stow to
    # not symlink the entire `.config/fish/functions` directory, which means fisher and any fisher
    # plugins don't end up tracked by git
end
